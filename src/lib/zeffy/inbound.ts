/**
 * Zeffy → HaBayit automatic sync (webhook, API reconcile).
 * Disabled: Chai Partner checkout still redirects to Zeffy; staff add donors via admin manual entry.
 */
export const ZEFFY_INBOUND_ENABLED = false;

export const ZEFFY_INBOUND_DISABLED_MESSAGE =
  'Zeffy automatic sync to HaBayit is disabled. Add Chai Partners manually in admin CRM.';
